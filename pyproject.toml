[project]
name = "pipeline-3d"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "jharif nelson aparicio casillas", email = "jharif.aparicio.casillas@usfx.bo" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
pipeline-3d = "pipeline_3d:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
